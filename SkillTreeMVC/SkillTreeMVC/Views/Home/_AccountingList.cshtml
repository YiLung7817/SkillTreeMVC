@model IEnumerable<SkillTreeMVC.Models.AccountingViewModel>
<div class="row">
    <div class="col-md-12">
        <table class="table table-bordered table-hover">
            <tr>
                <th>#</th>
                <th>類別</th>
                <th>日期</th>
                <th>金額</th>
            </tr>
            @{ 
                var count = 0;
                foreach (var item in Model)
                {
                    <tr>
                        <th>@(++count)</th>
                        <th>@item.Category</th>
                        <th>@item.Date.ToString("yyyy-MM-dd")</th>
                        <th>@item.Amount.ToString("###,###")</th>
                    </tr>
                    
                }
            }
        </table>
    </div>
</div>
